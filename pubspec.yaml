name: cazino
description: A privacy-aware, backend-ready two-player card game prototype.
publish_to: none
version: 0.1.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  test: ^1.28.0

flutter:
  uses-material-design: true
